"""
CyberQalqan AI — Cyber Security Advisor (Knowledge-Based Chat)
Answers cybersecurity questions using a built-in knowledge base.
No API keys required — all knowledge is embedded in code.
"""

import g4f
from typing import Dict, Any

# ─── System Instructions for LLM ────────────────────────────────────────

SYSTEM_PROMPT = """
Ты — CyberQalqan AI, умный ИИ-консультант по кибербезопасности из Казахстана.
Твоя задача — помогать пользователям защищать свои данные от мошенников, фишинга и взломов.

Твои основные правила:
1. Отвечай на том же языке, на котором к тебе обратились (Казахский, Русский или Английский).
2. Давай четкие, структурированные и короткие ответы (используй списки и эмодзи).
3. НИКОГДА не упоминай, что ты "языковая модель", "AI от OpenAI" или "ChatGPT". Ты — CyberQalqan AI.
4. Если пользователь спрашивает, что ты умеешь, отвечай, что можешь проверить письма, ссылки, телефонные номера и дать советы по безопасности (Instagram, пароли, банки, Wi-Fi).
5. Приводи примеры из казахстанских реалий, если это уместно (Kaspi, Halyk, eGov, OLX).

Вот базовая база знаний, на которую ты должен опираться:
- Взлом Instagram: Включить 2FA (Authenticator/SMS), сложные пароли (12+ символов).
- Фишинг: Поддельные сайты или SMS (например "Kaspi: ваша карта заблокирована").
- Wi-Fi: Не заходить в банк через публичный Wi-Fi без VPN.
- Карты: Не говорить никому CVV и SMS коды. Лучше открыть виртуальную карту для онлайн покупок.
- Дети в интернете: Использовать Family Link, не общаться с незнакомцами.
- Утечка данных: Проверять почту через haveibeenpwned.com, менять пароли.
"""

# Quick responses for common short phrases to save time
QUICK_RESPONSES = {
    "рақмет": {"kz": "Оқасы жоқ! 😊 Тағы сұрағыңыз болса, жазыңыз!", "ru": "Пожалуйста! 😊", "en": "You're welcome! 😊"},
    "спасибо": {"kz": "Оқасы жоқ! 😊", "ru": "Пожалуйста! Обращайтесь ещё! 😊", "en": "You're welcome! 😊"},
    "thanks": {"kz": "Оқасы жоқ! 😊", "ru": "Пожалуйста! 😊", "en": "You're welcome! Feel free to ask more! 😊"},
    "thank you": {"kz": "Оқасы жоқ! 😊", "ru": "Пожалуйста! 😊", "en": "You're welcome! 😊"},
    "көмек": {"kz": "Мен сізге көмектесуге дайынмын! Қандай сұрағыңыз бар?", "ru": "Я готов помочь! Какой у вас вопрос?", "en": "I'm ready to help! What's your question?"},
    "help": {"kz": "Мен көмектесуге дайынмын!", "ru": "Я готов помочь!", "en": "I'm ready to help! What's your question?"},
    "привет": {"kz": "Сәлеметсіз бе! Мен CyberQalqan AI-мын. Сізге қандай көмек керек?", "ru": "Здравствуйте! Я CyberQalqan AI. Чем могу помочь?", "en": "Hello! I am CyberQalqan AI. How can I help you?"},
    "сәлем": {"kz": "Сәлеметсіз бе! Мен CyberQalqan AI-мын. Сізге қандай көмек керек?", "ru": "Здравствуйте! Я CyberQalqan AI. Чем могу помочь?", "en": "Hello! I am CyberQalqan AI. How can I help you?"},
    "hello": {"kz": "Сәлеметсіз бе! Мен CyberQalqan AI-мын. Сізге қандай көмек керек?", "ru": "Здравствуйте! Я CyberQalqan AI. Чем могу помочь?", "en": "Hello! I am CyberQalqan AI. How can I help you?"},
}


from g4f.client import Client

def get_chat_response(message: str) -> Dict[str, any]:
    """Get a chat response for the given message using g4f (LLM)."""
    msg_lower = message.lower().strip()

    # Check quick responses first to save time and API calls
    for key, response in QUICK_RESPONSES.items():
        if key in msg_lower:
            return {
                "answer": response,
                "source": "CyberQalqan AI (Quick Response)",
            }

    try:
        # Call the g4f LLM provider via the new Client interface
        client = Client()
        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {"role": "system", "content": SYSTEM_PROMPT},
                {"role": "user", "content": message},
            ]
        )
        
        # Get the text content from the response
        response_text = response.choices[0].message.content
        
        # Clean up ads that some free g4f providers return
        ad_markers = [
            "Need proxies cheaper than the market?", 
            "https://op.wtf"
        ]
        for marker in ad_markers:
            if marker in response_text:
                response_text = response_text.split(marker)[0].strip()
        
        # Determine language vaguely based on input (fallback for JSON frontend if it requires a dict)
        if isinstance(response_text, str):
             answer_dict = {
                 "kz": response_text,
                 "ru": response_text,
                 "en": response_text
             }
        else:
             answer_dict = {
                 "kz": str(response_text),
                 "ru": str(response_text),
                 "en": str(response_text)
             }

        return {
            "answer": answer_dict,
            "source": "CyberQalqan LLM (g4f)",
        }
        
    except Exception as e:
        print(f"g4f Error: {e}")
        # Fallback if g4f fails (e.g. rate limit, no internet)
        fallback_msg = "Кешіріңіз, қазір менің серверімде жүктеме көп (LLM Error). Кішкене күте тұрыңыз. / Извините, сейчас большая нагрузка. Подождите немного."
        return {
            "answer": {
                "kz": fallback_msg,
                "ru": fallback_msg,
                "en": fallback_msg
            },
            "source": "CyberQalqan System Error"
        }


# Suggested questions for the chat UI
SUGGESTED_QUESTIONS = [
    {"kz": "Instagram аккаунтымды қалай қорғаймын?", "icon": "📸"},
    {"kz": "Телефоным бұзылды ма?", "icon": "📱"},
    {"kz": "Фишинг деген не?", "icon": "🎣"},
    {"kz": "Сенімді пароль қалай жасаймын?", "icon": "🔐"},
    {"kz": "Wi-Fi қауіпсіздігі", "icon": "📶"},
    {"kz": "Бұл сайт қауіпсіз бе?", "icon": "🌐"},
    {"kz": "VPN деген не?", "icon": "🌍"},
    {"kz": "2FA деген не?", "icon": "🔒"},
]

def analyze_call_transcript(transcript: str) -> Dict[str, Any]:
    """Analyze a transcribed phone call or voice message for vishing/scams."""
    
    analysis_prompt = f"""
Ты — CyberQalqan AI, эксперт по кибербезопасности.
Пользователь переслал тебе подозрительное голосовое сообщение, телефонный разговор или извлеченный текст из ВИДЕО (транскрипция ниже).
Твоя задача определить, является ли это экономическим мошенничеством (Вишингом), социальной инженерией, или это **Дипфейк (Deepfake) сгенерированный ИИ**.

Правила ответа:
1. Если это похоже на мошенничество или Дипфейк (например, звонок от ненастоящего "сотрудника банка" / знаменитость рекламирует "выигрыш" или "биржу"):
Начни ответ со слов: "🚨 Бұл психологиялық манипуляция, экономикалық алаяқтық (Вишинг) немесе жасанды интеллект жасаған Дипфейк болуы мүмкін." (На казахском), "🚨 Это психологическая манипуляция, экономическое мошенничество (Вишинг) или Дипфейк (Deepfake), созданный ИИ." (На русском).
Далее коротко объясни, в чем именно уловка обманщиков в этом тексте (например: "Знаменитости никогда не раздают деньги просто так" или "Банк никогда не просит СМС-код").

2. Если текст не похож на мошенничество (обычный разговор или неясный шум):
Ответь коротко: "✅ Бұл қарапайым хабарлама сияқты. Бірақ әрқашан сақ болыңыз!" (На казахском), "✅ Это похоже на обычное безопасное сообщение. Но всегда будьте бдительны!" (На русском).

Отвечай сразу на двух языках через абзац: сначала на казахском, затем на русском.

Транскрипция аудио/видео:
"{transcript}"
"""
    try:
        from g4f.client import Client
        client = Client()
        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {"role": "user", "content": analysis_prompt},
            ]
        )
        response_text = response.choices[0].message.content
        
        ad_markers = [
            "Need proxies cheaper than the market?", 
            "https://op.wtf"
        ]
        for marker in ad_markers:
            if marker in response_text:
                response_text = response_text.split(marker)[0].strip()
                
        answer_dict = {
            "kz": str(response_text),
            "ru": str(response_text),
            "en": str(response_text)
        }
        
        return {
            "answer": answer_dict,
            "source": "CyberQalqan LLM (Voice Analysis)",
        }
    except Exception as e:
        print(f"Voice Analysis LLM Error: {e}")
        fallback = "⚠️ Кешіріңіз, дауыстық хабарламаны сараптау мүмкін болмады. Бұл алаяқтық болуы әбден мүмкін, ешкімге СМС код немесе ақша жібермеңіз!"
        return {
            "answer": {
                "kz": fallback,
                "ru": fallback,
                "en": fallback
            },
            "source": "CyberQalqan System Error"
        }

def analyze_image_text(ocr_text: str) -> Dict[str, Any]:
    """Analyze text extracted from an image (OCR) for scam indicators."""
    
    if not ocr_text or len(ocr_text.strip()) < 5:
         fallback = "🖼️ Суреттегі мәтін өте аз немесе оқылмады. Егер бұл чек болса, оның түпнұсқалығын тексеріңіз. / Текст на фото слишком короткий или не читается. Если это чек, проверьте его подлинность."
         return {
             "answer": {
                 "kz": fallback,
                 "ru": fallback,
                 "en": fallback
             },
             "source": "CyberQalqan OCR Check"
         }

    analysis_prompt = f"""
Ты — CyberQalqan AI, эксперт по кибербезопасности.
Пользователь прислал скриншот или фото (например, банковский чек, сообщение от мошенников в WhatsApp, реклама выигрыша). Я извлек из него этот текст с помощью OCR (возможны небольшие опечатки распознавания).

Твоя задача определить, есть ли в этом тексте признаки мошенничества (поддельный чек об оплате Kaspi/Halyk, социальная инженерия, угрозы, обещание легких денег, лотерея).

Правила ответа:
1. Если текст явно мошеннический или похож на фейковый чек/скриншот перевода:
Начни ответ: "🚨 Назар аударыңыз! Суреттегі мәтіннен алаяқтық белгілері байқалды." (На казахском), "🚨 Внимание! В тексте на фото есть признаки мошенничества или подделки." (На русском).
Далее коротко объясни, что не так: например, "Сам факт скрина чека не гарантирует оплату, проверяйте поступление у себя в приложении" или "Это типичная схема с 'выигрышем'".

2. Если текст выглядит безопасно или это просто случайный текст/мем без угрозы:
Ответь: "✅ Суреттегі мәтін қауіпсіз сияқты. Бірақ чек болса, ақшаның түскенін өз қосымшаңыздан тексеріңіз." (На казахском), "✅ Текст на фото выглядит безопасно. Но если это чек об оплате, обязательно проверьте баланс в вашем банковском приложении." (На русском).

ОБЯЗАТЕЛЬНО Отвечай сразу на двух языках через абзац: сначала на казахском, затем на русском.

Распознанный текст с картинки:
"{ocr_text}"
"""
    try:
        from g4f.client import Client
        client = Client()
        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {"role": "user", "content": analysis_prompt},
            ]
        )
        response_text = response.choices[0].message.content
        
        ad_markers = [
            "Need proxies cheaper than the market?", 
            "https://op.wtf"
        ]
        for marker in ad_markers:
            if marker in response_text:
                response_text = response_text.split(marker)[0].strip()
                
        answer_dict = {
            "kz": str(response_text),
            "ru": str(response_text),
            "en": str(response_text)
        }
        
        return {
            "answer": answer_dict,
            "source": "CyberQalqan LLM (Image OCR Analysis)",
        }
    except Exception as e:
        print(f"Image Analysis LLM Error: {e}")
        fallback = "⚠️ Кешіріңіз, суреттегі мәтінді сараптауда қате шықты. Күмәндансаңыз, ешкімге ақша аудармаңыз. / Ошибка при анализе текста с фото. При сомнениях не переводите никому деньги."
        return {
            "answer": {
                "kz": fallback,
                "ru": fallback,
                "en": fallback
            },
            "source": "CyberQalqan System Error"
        }

def generate_phishing_simulation() -> Dict[str, Any]:
    """Generate a random mock phishing scenario for the training simulator."""
    import json
    
    analysis_prompt = """
Ты — CyberQalqan AI, генератор тренировочных фишинговых сценариев для обучения пользователей.
Твоя задача — сгенерировать ОДИН реалистичный фишинговый сценарий (SMS, Email или сообщение в мессенджере).

Выбери случайную тему из следующих:
1. Поддельное сообщение от Kaspi Bank или Halyk Bank о блокировке карты.
2. Поддельное письмо от "Отдела кадров" (HR) с требованием перейти по ссылке для ознакомления с новым графиком отпусков.
3. Сообщение в WhatsApp от "родственника", который просит срочно перевести деньги.
4. Выигрыш в лотерее или розыгрыше iPhone в Instagram.
5. Уведомление об оплате штрафа (СМС от Egov/КДГ), где ссылка ведет на поддельный сайт egov-pay.kz.

ОБЯЗАТЕЛЬНО ВЕРНИ ОТВЕТ В ВИДЕ ЧИСТОГО JSON формата без маркдауна (без ```json и ```). 
Структура JSON:
{
    "type": "sms" | "email" | "telegram" | "whatsapp",
    "sender": "Имя или номер отправителя",
    "message_kz": "Текст самого фишингового сообщения на казахском языке (максимально реалистично)",
    "message_ru": "Текст самого фишингового сообщения на русском языке (максимально реалистично)",
    "explanation_kz": "Объяснение, почему это фишинг и на что нужно было обратить внимание (на казахском)",
    "explanation_ru": "Объяснение, почему это фишинг и на что нужно было обратить внимание (на русском)"
}
"""
    try:
        from g4f.client import Client
        client = Client()
        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {"role": "user", "content": analysis_prompt},
            ]
        )
        response_text = response.choices[0].message.content
        
        # Clean up ads and markdown tags
        ad_markers = [
            "Need proxies cheaper than the market?", 
            "https://op.wtf",
            "```json",
            "```"
        ]
        for marker in ad_markers:
            if marker in response_text:
                response_text = response_text.replace(marker, "").strip()
                
        # Parse JSON
        import json
        scenario = json.loads(response_text)
        return scenario
        
    except Exception as e:
        print(f"Simulation Generation LLM Error: {e}")
        # Static fallback scenario if LLM fails
        return {
            "type": "sms",
            "sender": "Kaspi.kz",
            "message_kz": "Сіздің картаңыз бұғатталды. Бұғаттан шығару үшін сілтемеге өтіңіз: kaspl.kz/auth",
            "message_ru": "Ваша карта заблокирована попыткой взлома. Для отмены перейдите по ссылке: kaspl.kz/auth",
            "explanation_kz": "Бұл фишинг! Ресми Kaspi банкі ешқашан СМС арқылы сілтеме жібермейді және домен атауында қате бар (kaspi орнына kaspl).",
            "explanation_ru": "Это фишинг! Настоящий Kaspi не присылает ссылки в СМС для разблокировки карт. Обратите внимание на фальшивую ссылку (kaspl.kz вместо kaspi.kz)."
        }

